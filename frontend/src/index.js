import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Remove this line if you don't have or don't want an index.css file
// import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
